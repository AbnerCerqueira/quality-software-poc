version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: forms-api-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: forms_api
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
